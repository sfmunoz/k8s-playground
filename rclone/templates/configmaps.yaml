apiVersion: v1
kind: ConfigMap
metadata:
  name: rclone-script
data:
  rclone-null: |
    #!/bin/sh
    set -x
    exec tail -f /dev/null
  rclone-ls: |
    #!/bin/sh
    set -x
    date
    set -e
    set -o pipefail
    rclone ls rem: | \
      sort -k 2 -r | \
      awk '/pg_dump/ { print "rclone copyto rem:" $2 " /loc/" $2 ; exit 0 }'
    date
    exec tail -f /dev/null
