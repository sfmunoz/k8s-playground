{{- define "rclone.volumes" -}}
- name: {{ required ".volumeNameConf missing" .volumeNameConf }}
  secret:
    secretName: {{ required ".secretName missing" .secretName }}
    items:
    - key: {{ required ".secretKey missing" .secretKey }}
      path: rclone.conf
{{- with .volumes }}
{{- toYaml . | nindent 0 }}
{{- end }}
{{- end -}}
