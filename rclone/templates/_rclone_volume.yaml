{{- define "rclone.volume" -}}
- name: {{ required ".volName missing" .volName }}
  secret:
    secretName: {{ required ".secName missing" .secName }}
    items:
    - key: {{ required ".secKey missing" .secKey }}
      path: rclone.conf
{{- end -}}
