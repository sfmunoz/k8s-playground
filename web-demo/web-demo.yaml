apiVersion: v1
kind: Namespace
metadata:
  name: web-demo

---

apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: web-demo
  labels:
    name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "200m"
      requests:
        memory: "128Mi"
        cpu: "200m"
    ports:
      - containerPort: 80
